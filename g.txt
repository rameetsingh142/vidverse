// ignore_for_file: prefer_const_constructors, deprecated_member_use

import 'package:flutter/material.dart';
import 'package:solo/F_flutter_app_icons.dart';
import 'package:solo/my_flutter_app_icons.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      theme: ThemeData(primaryColor: Colors.black),
      debugShowCheckedModeBanner: false,
      home: Scaffold(body: Home()),
    );
  }
}

class Home extends StatelessWidget {
  const Home({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width,
      height: MediaQuery.of(context).size.height,
      decoration: BoxDecoration(
        image: DecorationImage(
          image: ExactAssetImage('assets/s.png'),
          fit: BoxFit.cover,
        ),
      ),
      child: Column(
        children: [
          SizedBox(
            height: 355,
          ),
          Image.asset('assets/SCP-Final-Stacked.png'),
          SizedBox(
            height: 240,
          ),
          RaisedButton(
            child: Text(
              "Next",
              style: TextStyle(fontSize: 16),
            ),
            onPressed: () {
              Navigator.push(
                context,
                MaterialPageRoute(builder: (context) => SecondRoute()),
              );
            },
            color: Color(0Xffdf7337),
            textColor: Colors.white,
            padding: EdgeInsets.symmetric(horizontal: 110, vertical: 20),
            splashColor: Colors.grey,
            shape: const RoundedRectangleBorder(
              borderRadius: BorderRadius.all(Radius.circular(35)),
            ),
          ),
          SizedBox(
            height: 50,
          ),
          Image.asset('assets/d.png')
        ],
      ),
    );
  }
}

class SecondRoute extends StatelessWidget {
  const SecondRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        width: MediaQuery.of(context).size.width,
        height: MediaQuery.of(context).size.height,
        decoration: BoxDecoration(
          image: DecorationImage(
            image: ExactAssetImage('assets/lo.png'),
            fit: BoxFit.cover,
          ),
        ),
        child: Column(
          children: [
            SizedBox(
              height: 90,
            ),
            Image.asset('assets/SCP-Final-Stacked.png',
                height: 130, width: 170),
            SizedBox(
              height: 90,
            ),
            Image.asset("assets/te.png"),
            SizedBox(height: 83),
            Container(
              child: Image.asset("assets/sig.png"),
              alignment: Alignment.centerLeft,
              padding: EdgeInsets.symmetric(horizontal: 50),
            ),
            SizedBox(
              height: 20,
            ),
            Column(
              // ignore: prefer_const_literals_to_create_immutables
              children: <Widget>[
                const Padding(
                  padding: EdgeInsets.symmetric(horizontal: 40, vertical: 10),
                  child: TextField(
                    style: TextStyle(color: Colors.white),
                    decoration: InputDecoration(
                        filled: true,
                        fillColor: Color(0Xff414141),
                        border: OutlineInputBorder(
                            borderRadius:
                                BorderRadius.all(Radius.circular(20.0)),
                            borderSide: BorderSide(color: Colors.grey)),
                        hintText: 'Enter a Email',
                        hintStyle: TextStyle(
                          color: Colors.white,
                        )),
                  ),
                ),
              ],
            ),
            SizedBox(
              height: 50,
            ),
            Container(
              child: Image.asset("assets/so.png"),
              alignment: Alignment.centerLeft,
              padding: EdgeInsets.symmetric(horizontal: 50),
            ),
            SizedBox(
              height: 25,
            ),
            RaisedButton.icon(
              onPressed: () {
                print('Button Clicked.');
              },
              shape: RoundedRectangleBorder(
                  borderRadius: BorderRadius.all(Radius.circular(11.0))),
              label: Text(
                'Signin with Google',
                style: TextStyle(color: Colors.white),
              ),
              icon: Icon(
                MyFlutterApp.google,
                color: Colors.white,
              ),
              padding: EdgeInsets.symmetric(horizontal: 98, vertical: 15),
              textColor: Colors.white,
              splashColor: Color(0Xff414141),
              color: Color(0Xff414141),
            ),
            SizedBox(
              height: 20,
            ),
            RaisedButton.icon(
              onPressed: () {
                print('Button Clicked.');
              },
              shape: RoundedRectangleBorder(
                  borderRadius: BorderRadius.all(Radius.circular(11.0))),
              label: Text(
                'Signin with Facebook',
                style: TextStyle(color: Colors.white),
              ),
              icon: Icon(
                MyFlutterApp.google,
                color: Colors.white,
              ),
              padding: EdgeInsets.symmetric(horizontal: 90, vertical: 15),
              textColor: Colors.white,
              splashColor: Color(0Xff414141),
              color: Color(0Xff414141),
            ),
            SizedBox(
              height: 20,
            ),
          ],
        ),
      ),
    );
  }
}
